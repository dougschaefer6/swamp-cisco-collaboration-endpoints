manifestVersion: 1
name: "@dougschaefer/cisco-collaboration-endpoints"
version: "2026.04.02.1"
description: "Cisco RoomOS device and macro management via Webex Control Hub — device inventory, health checks, workspace management, xAPI command execution, status queries, configuration management, macro lifecycle (save, activate, deploy, fleet push), and MTR-aware operations. Vault-based credential resolution."
repository: https://github.com/dougschaefer6/swamp-cisco-collaboration-endpoints
labels:
  - cisco
  - webex
  - roomos
  - macros
  - av
  - conferencing
models:
  - cisco/device.ts
  - cisco/macro.ts
